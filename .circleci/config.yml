version: 2
jobs:
  build:
    docker:
      — image: circleci/node:7.10
    # working_directory: ~/repo
    steps:
      — checkout
      - run: echo "A first hello"
      # - run: npm install
      # — run: npm test
